name: taskweaver
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch
    - transformers
    - datasets
    - tqdm
    - accelerate
    - huggingface_hub
    - ipykernel
